# TODO

Consolidate these old TODOS, should we keep any for the rewrite?
- bundle post assets in a smarter way
- get rid of Reads page?
- heading anchors
- check Accept header
- use [image map](https://www.devever.net/~hl/imagemap-example-client#cs-balloon) somewhere?

We will restructure and refactor this project as needed to fit the descriptions of `go-web-project`.
I will continue with the /spec command next.

/spec use `interview-me` and `idea-refine`, then `spec-driven-development` to create a rewrite spec

/plan use `planning-and-task-breakdown` to make tasks from the spec

/test add tests for what is worth testing right now before we start the rewrite.

/build implement the `go-web-project` with `frontend-ui-engineering` for the looks.

/review

/test
